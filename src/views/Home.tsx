import OpeningArticle from '../components/OpeningArticle';

export const Home = (): JSX.Element => {
  return (
    <section className=''>
      <OpeningArticle />
    </section>
  );
};

export default Home;
